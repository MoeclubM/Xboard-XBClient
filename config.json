{
  "name": "Xboard-XBClient",
  "code": "xbclient",
  "version": "0.0.4",
  "description": "为 XBClient 客户端提供 Xboard 服务端对接能力：下发广告与套餐页开关，验证 AdMob 激励广告 SSV 回调并通过礼品卡模板发放奖励。",
  "author": "MoeclubM",
  "type": "feature",
  "require": {
    "xboard": ">=1.0.0"
  },
  "config": {
    "enable_reward_ads": {
      "type": "boolean",
      "default": true,
      "label": "开启 App 激励广告",
      "description": "关闭后 App 不展示看广告入口，SSV 回调也不会发放奖励。"
    },
    "enable_app_payment": {
      "type": "boolean",
      "default": true,
      "label": "开启 App 套餐页",
      "description": "下发给 App 控制套餐页是否展示；开启后 App 点击具体套餐会跳转 Xboard 网页套餐支付流程，不在 App 内置验单。"
    },
    "rewarded_ad_unit_id": {
      "type": "string",
      "default": "",
      "label": "AdMob 激励广告单元 ID",
      "description": "服务端下发给 App，并用于校验 SSV 回调；填写完整广告单元 ID，例如 ca-app-pub-xxx/yyy。"
    },
    "ssv_secret": {
      "type": "string",
      "default": "",
      "label": "客户端 SSV 令牌签名密钥",
      "description": "用于给 App 获取的 custom_data 做 HMAC 签名。请使用随机 32 字节以上密钥，不要公开。"
    },
    "gift_card_template_id": {
      "type": "string",
      "default": "",
      "label": "广告奖励礼品卡模板 ID",
      "description": "必填。SSV 验证通过后，插件会基于该模板创建 max_usage=1 的临时兑换码并立即为当前用户兑换；余额、流量、套餐等奖励均由礼品卡模板决定。"
    },
    "token_ttl_seconds": {
      "type": "string",
      "default": "900",
      "label": "SSV 令牌有效期秒数",
      "description": "App 获取 custom_data 后需在有效期内完成广告观看。"
    },
    "callback_max_age_seconds": {
      "type": "string",
      "default": "3600",
      "label": "SSV 回调最大时间偏移秒数",
      "description": "Google 回调 timestamp 与服务器当前时间差超过该值将拒绝。"
    }
  }
}
